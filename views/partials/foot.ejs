<% if (typeof embed !== 'undefined' && embed) { %>
<% } else { %>
    </main>
<% if (typeof user !== 'undefined' && user) { %>
  </div><!-- .content -->
</div><!-- .app -->
<% } %>
</body>
</html>
<% } %>
